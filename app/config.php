<?php

declare(strict_types=1);

/*
 * Connection parameters supplied for the local Wamp installation.
 * Change only these values when the site is deployed elsewhere.
 */
const DB_HOST = 'localhost';
const DB_NAME = 'quorion';
const DB_USER = 'root';
const DB_PASS = '';

const ROOT_PATH = __DIR__ . '/..';
